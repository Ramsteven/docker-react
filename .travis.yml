sudo: required
services: 
  - docker

before_install:
  - docker build -t ramsteven/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ramsteven/docker-react npm run test -- --coverage